<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<style>
  .category-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
  }

  .category-table {
    background: white;
    border-radius: 0.5rem;
    overflow: hidden;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  }

  .table thead {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
  }

  .table thead th {
    border: none;
    padding: 1rem;
    font-weight: 500;
  }

  .table tbody td {
    padding: 1rem;
    vertical-align: middle;
  }

  .action-buttons {
    display: flex;
    gap: 0.5rem;
  }

  .btn-sm {
    padding: 0.375rem 0.75rem;
    font-size: 0.875rem;
  }
</style>
<body>
  <div class="category-header">
    <div>
      <h1 class="page-title mb-2">จัดการหมวดหมู่</h1>
      <p class="text-muted">รายการหมวดหมู่อุปกรณ์ทั้งหมด</p>
         <a href="/admin/equipmentAdmin" class="btn btn-outline-primary">
            <i class="bi bi-arrow-left me-2"></i>กลับไปหน้าอุปกรณ์
        </a>
    </div>
    <div class="d-flex gap-2">
        <a href="/admin/trash/categories" class="btn btn-outline-danger">
            <i class="bi bi-trash me-2"></i>ถังขยะ
        </a>
      <a href="/admin/categories/add" class="btn btn-primary">
        <i class="bi bi-plus-circle me-2"></i>เพิ่มหมวดหมู่
      </a>
    </div>
  </div>

  <% if (locals.alert) { %>
    <div class="alert alert-<%= alert.type %> alert-dismissible fade show" role="alert">
      <strong><%= alert.title %></strong> <%= alert.message %>
      <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
  <% } %>

  <div class="category-table">
    <table class="table table-hover mb-0">
      <thead>
        <tr>
          <th width="10%">#</th>
          <th>ชื่อหมวดหมู่</th>
          <th width="15%">วันที่สร้าง</th>
          <th width="15%">การจัดการ</th>
        </tr>
      </thead>
      <tbody>
        <% if (categories && categories.length > 0) { %>
          <% categories.forEach((category, index) => { %>
            <tr>
              <td><strong><%= index + 1 %></strong></td>
              <td><%= category.name %></td>
              <td><%= new Date(category.created_at).toLocaleDateString('th-TH') %></td>
              <td>
                <div class="action-buttons">
                  <a href="/admin/categories/edit/<%= category._id %>" class="btn btn-warning btn-sm">
                    <i class="bi bi-pencil"></i>
                  </a>
                  <form action="/admin/categories/delete/<%= category._id %>" method="POST" style="display: inline;" onsubmit="return confirm('คุณแน่ใจหรือไม่ที่จะลบหมวดหมู่นี้?')">
                    <button type="submit" class="btn btn-danger btn-sm">
                      <i class="bi bi-trash"></i>
                    </button>
                  </form>
                </div>
              </td>
            </tr>
          <% }) %>
        <% } else { %>
          <tr>
            <td colspan="4" class="text-center py-5">
              <i class="bi bi-folder-x" style="font-size: 3rem; color: #6c757d;"></i>
              <p class="mt-3 text-muted">ยังไม่มีหมวดหมู่ในระบบ</p>
              <a href="/admin/categories/add" class="btn btn-primary mt-2">
                <i class="bi bi-plus-circle me-2"></i>เพิ่มหมวดหมู่แรก
              </a>
            </td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>